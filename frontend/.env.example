# Browser requests use this same-origin path. Next.js rewrites it to the backend.
NEXT_PUBLIC_API_URL=/api/backend

# The backend is intentionally separate from the Next.js process in local development.
BACKEND_INTERNAL_URL=http://localhost:3001
